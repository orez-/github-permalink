# github-permalink

```
usage: github_permalink.py [-h] url timestamp

convert a GitHub code link into a permalink, retroactively

positional arguments:
  url         the link to convert
  timestamp   the estimated date and time at which the link was created

optional arguments:
  -h, --help  show this help message and exit
```
